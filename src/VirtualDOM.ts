import VirtualNode from "./VirtualNode";

export default class VirtualDOM {
    constructor() {
        var bla = new VirtualNode();
        return;
    }

    public bla(params: number): number {
        return 0;
    }
}
